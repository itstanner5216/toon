# toon-plus

Intelligent LLM context compression with the `toon` import package and
standalone `engines` for BMX+ and SageRank.

```python
from toon import compress, encode_output
from engines import BMXPlusIndex, SageRank
```
